
***********************************************
* Just add these lines in your ~/.bashrc file *
***********************************************

if [ -d "$HOME/_" ] ; then
    PATH="$HOME/_/bin:$PATH"
fi

if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi
